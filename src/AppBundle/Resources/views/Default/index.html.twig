{% extends 'base.html.twig' %}

{% block title %}Euro 2016{% endblock %}

{% block body %}
    {{ parent() }}
    <table class="table table-bordered table-hover">
    <tr>
        <th>POT 1</th>
        {% for item in pot_one %}
           <td>
               <a href="{{ path('show_country', {'slug': item.slug}) }}">
                   <img src="{{ item.flag }}">
               </a>
               <a href="{{ path('show_team', {'slug': item.team.slug }) }}">
                   {{ item.name }}
               </a>
           </td>
        {% endfor %}
    </tr>
    <tr>
        <th>POT 2</th>
        {% for item in pot_two %}
            <td>
                <a href="{{ path('show_country', {'slug': item.slug}) }}">
                    <img src="{{ item.flag }}">
                </a>
                <a href="{{ path('show_team', {'slug': item.team.slug}) }}">
                    {{ item.name }}
                </a>
            </td>
        {% endfor %}
    </tr>
    <tr>
        <th>POT 3</th>
        {% for item in pot_three %}
            <td>
                <a href="{{ path('show_country', {'slug': item.slug}) }}">
                    <img src="{{ item.flag }}">
                </a>
                <a href="{{ path('show_team', {'slug': item.team.slug}) }}">
                    {{ item.name }}
                </a>
            </td>
        {% endfor %}
    </tr>
    <tr>
        <th>POT 4</th>
        {% for item in pot_four %}
            <td>
                <a href="{{ path('show_country', {'slug': item.slug}) }}">
                    <img src="{{ item.flag }}">
                </a>
                <a href="{{ path('show_team', {'slug': item.team.slug}) }}">
                    {{ item.name }}
                </a>
            </td>
        {% endfor %}
    </tr>
    </table>


{% endblock %}