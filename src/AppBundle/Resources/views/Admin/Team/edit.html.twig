{% extends 'admin.html.twig' %}

{% block body %}
    {{ parent() }}
    <h1>Edit team</h1>
    <div>

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <input type="submit" value="Update"
               class="btn btn-default pull-right" />
        {{ form_end(form) }}
    </div>
{% endblock %}

